import React, { Component } from "react";
import styled from "styled-components";

export default class App extends Component {
  render() {
    return (
      <Wrapper>
        <CalcWrapper>Hello</CalcWrapper>
      </Wrapper>
    );
  }
}

const Wrapper = styled.div``;

const CalcWrapper = styled.div``;
